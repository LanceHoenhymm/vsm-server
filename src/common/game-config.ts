const tradingRoundDuration = 20 * 60; // 20 minutes
const gameInitDelay = 10 * 60; // 10 minute
const maxGameRounds = 5;

const startingAmount = 1000;
const startingValuation = 0;

const muftMoneyAwarded = 10000;

export {
  startingAmount,
  startingValuation,
  maxGameRounds,
  muftMoneyAwarded,
  tradingRoundDuration,
  gameInitDelay,
};
