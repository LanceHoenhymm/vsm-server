const gameStartDelay = 10 * 60;
const tradingRoundDuration = 10 * 60;
const maxGameRounds = 5;

const startingAmount = 1000;

export { startingAmount, maxGameRounds, tradingRoundDuration, gameStartDelay };
